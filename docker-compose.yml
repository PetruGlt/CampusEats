version: '3.4'

services:
  backend:
    build:
      context: .
      dockerfile: CampusEats/Dockerfile
    ports:
      - "8080:80"

  frontend:
    build:
      context: .
      dockerfile: CampusEatsUI/Dockerfile
    ports:
      - "3000:80"
