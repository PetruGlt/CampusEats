@page "/Kitchens"


<PageTitle>CampusEats - Kitchen</PageTitle>

<MudGrid>
    <MudItem xs="12">
        <MudText Typo="Typo.h4" GutterBottom="true">Kitchen Dashboard</MudText>
        
        <MudTabs>
            <MudTab Text="Pending Orders">
                <MudGrid>
                    @*@foreach (var order in PendingOrders)
                    {
                        <MudItem xs="12" md="6" Class="mb-4">
                            <MudCard>
                                <MudCardContent>
                                    <MudText Typo="Typo.h6">Order #@order.OrderNumber</MudText>
                                    <MudText Typo="Typo.body2">Placed: @order.CreatedAt.ToString("t")</MudText>
                                    
                                    <MudList Class="my-2">
                                        @foreach (var item in order.Items)
                                        {
                                            <MudListItem Text="@item.Quantity x @item.Name" />
                                        }
                                    </MudList>
                                </MudCardContent>
                                <MudCardActions>
                                    <MudButton 
                                        Variant="Variant.Filled" 
                                        Color="Color.Primary"
                                        OnClick="@(() => UpdateOrderStatus(order.Id, OrderStatus.Preparing))">
                                        Start Preparing
                                    </MudButton>
                                </MudCardActions>
                            </MudCard>
                        </MudItem>
                    }*@
                </MudGrid>
            </MudTab>
            
            <MudTab Text="In Progress">
                <!-- Similar structure for in-progress orders -->
            </MudTab>
        </MudTabs>
    </MudItem>
</MudGrid>

@code {
    
}