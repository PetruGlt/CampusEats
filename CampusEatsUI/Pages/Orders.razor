@page "/orders"

<PageTitle>CampusEats - My Orders</PageTitle>

<MudTabs>
    <MudTab Text="Current Orders">
        @*<MudTable Items="@CurrentOrders" Hover="true" Breakpoint="Breakpoint.Sm">
            <HeaderContent>
                <MudTh>Order #</MudTh>
                <MudTh>Items</MudTh>
                <MudTh>Total</MudTh>
                <MudTh>Status</MudTh>
                <MudTh>Actions</MudTh>
            </HeaderContent>
            <RowTemplate>
                $1$<MudTd>@context.OrderNumber</MudTd>
                <MudTd>@context.Items.Count items</MudTd>
                <MudTd>$@context.TotalAmount</MudTd>
                <MudTd>
                    <MudChip Color="@GetStatusColor(context.Status)" Size="Size.Small">
                        @context.Status
                    </MudChip>
                </MudTd>#1#
                $1$<MudTd>
                    @if (context.Status == OrderStatus.Pending)
                    {
                        <MudButton 
                            Variant="Variant.Text" 
                            Color="Color.Error" 
                            Size="Size.Small"
                            OnClick="@(() => CancelOrder(context.Id))">
                            Cancel
                        </MudButton>
                    }
                </MudTd>#1#
            </RowTemplate>
        </MudTable>*@
    </MudTab>
    
    <MudTab Text="Order History">
        @*<MudTable Items="@OrderHistory" Hover="true">
            <!-- Similar structure for history -->
        </MudTable>*@
    </MudTab>
</MudTabs>

@code {
    
}